general {
    before_sleep_cmd = bash -c "pw-cli ls Node | grep -q 'WEBRTC VoiceEngine' && echo 'Call active, skipping suspend' && exit 0"
    after_sleep_cmd = hyprctl dispatch dpms on
    ignore_dbus_inhibit = false
    ignore_systemd_inhibit = false
}

listener {
    timeout = 600
    on-timeout = bash -c "pw-cli ls Node | grep -q 'WEBRTC VoiceEngine' && echo 'Call active, skipping suspend listener' && exit 0; systemctl suspend"
    on-resume = ""
}
